{
  "name": "@yishneg90/button",
  "description": "Button component",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "webpack -p --config ../../webpack.config.js ../../babel.config.js",
    "clean": "rimraf dist && rimraf node_modules && rimraf package-lock.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "peerDependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "@types/react": "^16.9.25",
    "@types/react-dom": "^16.9.5"
  }
}
